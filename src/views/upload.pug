extends base.pug
block content
  if(errorMessage)
    span=errorMessage
  form(method="POST", action="/videos/upload" ) 
    input(type="text", name="title", placeholder="Write newTitle")
    input(type="text", name="description", placeholder="Wrtie description")
    input(type="text", name="hashtags", placeholder="Write hashtags, seperate a comma")
    input(type="submit", value="save")