doctype html 
html(lang="kr")
  head
    link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    title #{titleContent} | youtube
  body 
    header 
      h1=titleContent
      nav
        ul 
          if(!req.session.logedIn)
            li 
              a(href="/join") Join
            li
              a(href="login") Login
          li 
            a(href="/videos/upload") Update Video
          li 
            a(href="/") Home
          li 
            a(href="/search") Search
    main
      block content
  include partials/footer.pug